import { IsFreeFilterPipe } from './isFree-filter.pipe';

describe('IsFreeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFreeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
